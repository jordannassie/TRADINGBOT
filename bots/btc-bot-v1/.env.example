# ─── Supabase (Terminal bot only — service role key stays local) ───────────────
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# ─── Polymarket CLOB ───────────────────────────────────────────────────────────
# Same variable names as the main app so a single .env works for both if needed.
VITE_POLY_API_KEY=
VITE_POLY_API_SECRET=
VITE_POLY_API_PASSPHRASE=
VITE_POLY_PROXY_WALLET=

# ─── Safety ───────────────────────────────────────────────────────────────────
# DRY_RUN=true  → LIVE broker logs "would place order" but does NOT send.
# DRY_RUN=false → LIVE broker sends real orders (only in LIVE mode + executionEnabled).
DRY_RUN=true

# Extra guard: must also be true in Supabase btc_bot_config for real execution.
EXECUTION_ENABLED_DEFAULT=false
